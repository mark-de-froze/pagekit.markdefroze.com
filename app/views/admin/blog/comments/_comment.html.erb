<article class="uk-comment" id="comment<%=comment.id%>">
  <header class="uk-comment-header uk-grid-medium uk-flex-middle" uk-grid>
    <div class="uk-width-auto">
      <img class="uk-comment-avatar" src="<%= comment.gravatar_url %>" width="80" height="80" alt="">
    </div>
    <div class="uk-width-expand">
      <h4 class="uk-comment-title uk-margin-remove"><a class="uk-link-reset" href="#"><%= comment.from_name %></a></h4>
      <ul class="uk-comment-meta uk-subnav uk-subnav-divider uk-margin-remove-top uk-margin-remove-bottom">
        <li><a href="#"><%= time_ago_in_words(comment.created_at) %> <%=t(:ago, default: 'ago')%></a></li>
        <li><%= link_to t(:edit), edit_blog_comment_path(comment) %></li>
        <li><%= link_to t(:destroy), comment, method: :delete, data: { confirm: t(:confirm) } %></li>
      </ul>
      <% unless comment.status == 'published' %>
      <span class="uk-badge"><%= comment.status_text %></span>
      <% end %>
    </div>
  </header>
  <div class="uk-comment-body">
    <p><%= comment.comment %></p>
  </div>
</article>
